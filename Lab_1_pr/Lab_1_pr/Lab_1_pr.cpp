#include <Windows.h>
using namespace System;
/*
int main(array<System::String^>^ args)
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	String^ inputString = L"Продукти в АТБ: молоко, хліб, картопля, яйця.";
		Console::WriteLine(L"Початковий рядок: {0}", inputString);
	int colonPosition = inputString->IndexOf(L':');
	if (colonPosition != -1) {
		String^ result = inputString->Substring(0, colonPosition);
		Console::WriteLine(L"Рядок до першої двокрапки: {0}", result);
	}
	else {
		Console::WriteLine(L"Двокрапка не знайдена.");
	}

	return 0;
}
*/

/*
int main(array<System::String^>^ args)
{
SetConsoleCP(1251);
SetConsoleOutputCP(1251);
String^ inputString = L"Тестовий, рядок, з, комами.";
Console::WriteLine(L"Початковий рядок: {0}", inputString);
int lastCommaPosition = inputString->LastIndexOf(L',');
if (lastCommaPosition != -1) {
Console::WriteLine(L"Позиція останньої коми: {0}",
lastCommaPosition + 1);
}
else {
Console::WriteLine(L"Кома не знайдена.");
}
return 0;
}
*/